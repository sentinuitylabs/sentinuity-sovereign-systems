# Security policy

Do not open public issues containing secrets, wallet material, private RPC URLs, bot tokens, database snapshots or live transaction details.

Before publishing a report, remove credentials and sensitive identifiers. Rotate any credential that has appeared in a log or URL. Live trading should remain disabled during investigation unless the operator has independently verified the risk boundary.
