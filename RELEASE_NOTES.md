# Release candidate — 28 July 2026

## Runtime

- Retains runner-aware oracle writes and runner-aware executor evaluation from the latest supplied codebase.
- Adds bounded lock backoff and concise contention telemetry to `intelligence_orchestrator.py`.
- Adds bounded lock backoff and slower ordinary cadence to `macro_channel.py`.
- Adds lane-aware lock handling, smaller weave batches and heartbeat isolation to `ingest_pipeline.py`.
- Does not loosen entry thresholds, hard-stop authority, chain-fill truth or live safety boundaries.

## Identity

- Adds the crystalline chaos-to-order Sentinuity identity to the sovereign header and System Identity containment.
- Preserves matrix rain and the existing command hierarchy.
- Adds public brand assets under `assets/brand/` for GitHub, social previews and project pages.

## Public repository hygiene

- Removes compiled Python caches, nested repair ZIPs and timestamped backup source files.
- Adds root documentation, contribution and security guidance, environment template and CI compile checks.
- Excludes databases, logs, secrets, wallet material, audit outputs and local backups by default.

## Verification boundary

Static verification and Python compilation are release gates. Runtime edge restoration still requires a paper-only canary with healthy primary price truth and materially reduced lock frequency before live-money consideration.
